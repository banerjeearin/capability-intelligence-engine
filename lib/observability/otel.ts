/**
 * Lightweight OpenTelemetry-compatible bootstrap placeholder.
 * In production, replace with @opentelemetry/sdk-node registration.
 */
export function initOpenTelemetry() {
  // no-op bootstrap for now; keeps a stable integration point.
  return true;
}
